---
- name: Update Development Environment
  hosts: localhost
  become: false
  gather_facts: true
  
  vars:
    user_home: "{{ ansible_env.HOME }}"
    
  tasks:
    - name: Update all development tools
      include_tasks: tasks/updates.yml
      
    - name: Display update summary
      debug:
        msg: |
          🎉 Development Environment Update Complete!
          
          Updated tools:
          - ✅ System packages (Homebrew/apt)
          - ✅ Node.js (to latest LTS)
          - ✅ Bun (to latest)
          - ✅ Go (to latest)
          - ✅ kubectl (to latest)
          - ✅ Helm (to latest)
          - ✅ k9s (to latest)
          - ✅ Global npm packages
          - ✅ VS Code extensions
          - ✅ Bash completion files
          
          🔄 Please restart your terminal to ensure all updates are active. 